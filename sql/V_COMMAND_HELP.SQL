CREATE OR REPLACE VIEW V_COMMAND_HELP AS
SELECT 
    jsc.SERVER_ID 
    , sv.UID
    , jsc.COMMAND_ID 
    , cm.HELP
FROM JOIN_SERVERS_COMMANDS jsc
LEFT JOIN COMMANDS cm ON
    jsc.COMMAND_ID = cm.COMMAND_ID
LEFT JOIN SERVERS sv ON
    jsc.SERVER_ID = sv.SERVER_ID
    
