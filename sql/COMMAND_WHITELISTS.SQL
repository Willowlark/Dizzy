DROP TABLE COMMAND_WHITELISTS;

CREATE TABLE COMMAND_WHITELISTS (
        ID int NOT NULL UNIQUE
        , AUTHOR VARCHAR(100)
        , ALLOWED BOOLEAN -- Implicit deny on COMMAND with COMMAND_ID, TRUE is allowed
        , PRIMARY KEY (ID)
);

INSERT INTO COMMAND_WHITELISTS (ID, AUTHOR, ALLOWED) VALUES
        (1, 'Willowlark', TRUE)
;